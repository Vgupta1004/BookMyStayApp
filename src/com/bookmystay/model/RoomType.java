
package com.bookmystay.model;

/**
 * Represents the categories of rooms available in the BookMyStay system.
 * Using an enum prevents inconsistent data entry and serves as the key 
 * for inventory and pricing lookups
 */
public enum RoomType {
	SINGLE, DOUBLE, SUITE
}
